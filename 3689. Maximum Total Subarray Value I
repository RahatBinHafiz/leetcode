class Solution {
public:
    #define ll long long int
    long long maxTotalValue(vector<int>& nums, int k) {
        ll big = *max_element(nums.begin(), nums.end());
        ll low = *min_element(nums.begin(), nums.end());
        return k*(big - low);
    }
};
