class Solution {
public:
   int fibb(int n)
{
    if(n<=1) return n; 
    // int last = fibb(n-1);
    // int slast = fibb(n-2);
    // return last + slast;
    return fibb(n-1)+fibb(n-2);
}
    int fib(int n) {
        return fibb(n);
    }
};
