class Solution {
public:
    int findFinalValue(vector<int>& nums, int original) {
        int n = nums.size();
        map<int,int>mp;
        for(auto& i:nums) mp[i]++;
        int ans = original;
        while(mp.count(ans))
        {
            ans*=2;
        }
        return ans;
    }
};
